# Project Overview

This project is focused on cataloging fashion and clothing items. More details and clarifications regarding its full functionality will be announced soon.

## How to Run

To build and install the project, run the following command in your terminal:

```sh
mvn clean install
```

## TODO

The following tasks still need to be completed:
- Proper code documentation
- Add an option to remove an employee from the database
- Create test code to be placed in the `src/main/test` folder for use with `mvn test`
